name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  DEVELOPER_DIR: /Applications/Xcode.app/Contents/Developer

jobs:
  build-and-test:
    name: Build & Test
    runs-on: macos-14

    steps:
      # Checkout code
      - name: Checkout
        uses: actions/checkout@v6

      # Setup environment
      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      # Install dependencies (customize for your project)
      # - name: Install Dependencies
      #   run: brew install <your-dependencies>

      - name: Install SwiftLint
        run: brew install swiftlint

      # Run SwiftLint
      - name: SwiftLint
        run: |
          echo "📝 Running SwiftLint..."
          LINT_RESULT=$(swiftlint lint --reporter github-actions-logging 2>&1) || true
          echo "$LINT_RESULT"

          # Count errors (serious violations)
          ERROR_COUNT=$(echo "$LINT_RESULT" | grep -c "error:" || echo "0")
          WARNING_COUNT=$(echo "$LINT_RESULT" | grep -c "warning:" || echo "0")

          echo ""
          echo "📊 SwiftLint Results: $ERROR_COUNT errors, $WARNING_COUNT warnings"

          # Fail only on errors, not warnings (strict mode enabled)
          if [ "$ERROR_COUNT" -gt 0 ]; then
            echo "❌ SwiftLint found $ERROR_COUNT error(s) - failing build"
            exit 1
          fi
          echo "✅ SwiftLint check complete"

      # Cache DerivedData for faster builds
      - name: Cache DerivedData
        uses: actions/cache@v5
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: deriveddata-${{ runner.os }}-${{ hashFiles('**/*.swift') }}
          restore-keys: |
            deriveddata-${{ runner.os }}-

      # Install xcpretty if not available
      - name: Install xcpretty
        run: |
          if ! command -v xcpretty &> /dev/null; then
            gem install xcpretty
          fi

      # Build the project (customize project/scheme names)
      - name: Build
        run: |
          xcodebuild build \
            -project YourProject.xcodeproj \
            -scheme YourScheme \
            -destination 'platform=macOS' \
            | xcpretty

      # Run tests with code coverage
      - name: Run Tests with Coverage
        run: |
          xcodebuild test \
            -project YourProject.xcodeproj \
            -scheme YourScheme \
            -destination 'platform=macOS' \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults \
            | xcpretty --report junit --output build/reports/junit.xml

      # Extract and enforce code coverage (blocking gate)
      - name: Generate Coverage Report
        id: coverage
        run: |
          # Find the xcresult bundle
          RESULT_BUNDLE="TestResults.xcresult"
          THRESHOLD=80

          if [ -d "$RESULT_BUNDLE" ]; then
            echo "📊 Code Coverage Report"
            echo "========================"

            # Get coverage percentage
            if xcrun xccov view --report --only-targets "$RESULT_BUNDLE" > coverage_report.txt 2>&1; then
              # Display coverage summary
              cat coverage_report.txt

              # Extract overall coverage percentage (customize app name)
              COVERAGE=$(grep "YourApp.app" coverage_report.txt | awk '{print $4}' | tr -d '%' || echo "")

              if [ ! -z "$COVERAGE" ]; then
                echo ""
                echo "📈 Coverage: ${COVERAGE}%"
                echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT

                # Save coverage to file for artifact
                echo "$COVERAGE" > coverage_percentage.txt

                # Enforce threshold (80% minimum - BLOCKING)
                COVERAGE_INT=${COVERAGE%.*}
                if [ "$COVERAGE_INT" -lt "$THRESHOLD" ]; then
                  echo ""
                  echo "❌ Coverage ($COVERAGE%) is below required threshold ($THRESHOLD%)"
                  echo "   Add more tests to improve coverage before merging."
                  exit 1
                else
                  echo "✅ Coverage ($COVERAGE%) meets threshold ($THRESHOLD%)"
                fi
              else
                echo "⚠️ Could not extract coverage percentage - allowing build to continue"
              fi
            else
              echo "⚠️ Coverage data not available in result bundle"
              echo "   This can happen if code coverage collection failed during test run"
              cat coverage_report.txt 2>/dev/null || true
            fi
          else
            echo "⚠️ No test results found at $RESULT_BUNDLE"
          fi

      # Generate detailed coverage JSON (non-blocking)
      - name: Generate Coverage JSON
        if: always()
        continue-on-error: true
        run: |
          RESULT_BUNDLE="TestResults.xcresult"
          if [ -d "$RESULT_BUNDLE" ]; then
            xcrun xccov view --report --json "$RESULT_BUNDLE" > coverage.json 2>&1 || echo "{}" > coverage.json
          fi

      # Check version consistency
      - name: Version Check
        run: |
          chmod +x ./scripts/version-check.sh
          ./scripts/version-check.sh

      # Upload test results and coverage
      - name: Upload Test Results & Coverage
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: test-results
          path: |
            build/reports/
            TestResults.xcresult/
            coverage_report.txt
            coverage_percentage.txt
            coverage.json
          retention-days: 14

      # Post coverage summary to PR (if PR)
      - name: Coverage Summary
        if: github.event_name == 'pull_request'
        run: |
          if [ -f coverage_percentage.txt ]; then
            COVERAGE=$(cat coverage_percentage.txt)
            echo "### 📊 Code Coverage: ${COVERAGE}%" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "See artifacts for detailed coverage report." >> $GITHUB_STEP_SUMMARY
          else
            echo "### ⚠️ Code Coverage: Not Available" >> $GITHUB_STEP_SUMMARY
          fi

  # Code Complexity Check
  complexity:
    name: Complexity Check
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run Complexity Check
        id: complexity
        run: |
          chmod +x ./scripts/complexity-check.sh

          # Run complexity check and capture output (strip ANSI codes)
          ./scripts/complexity-check.sh --warn-only 2>&1 | sed 's/\x1b\[[0-9;]*m//g' | tee complexity_output.txt

          # Extract counts (use head -1 to ensure single value)
          ERRORS=$(grep "Errors:" complexity_output.txt | grep -oE '[0-9]+' | head -1 || echo "0")
          WARNINGS=$(grep "Warnings:" complexity_output.txt | grep -oE '[0-9]+' | head -1 || echo "0")
          FILES=$(grep "Files checked:" complexity_output.txt | grep -oE '[0-9]+' | head -1 || echo "0")

          # Ensure we have valid numbers (default to 0 if empty)
          ERRORS="${ERRORS:-0}"
          WARNINGS="${WARNINGS:-0}"
          FILES="${FILES:-0}"

          echo "errors=$ERRORS" >> $GITHUB_OUTPUT
          echo "warnings=$WARNINGS" >> $GITHUB_OUTPUT
          echo "files=$FILES" >> $GITHUB_OUTPUT

          # Save metrics for tracking
          mkdir -p metrics
          echo "{\"errors\": $ERRORS, \"warnings\": $WARNINGS, \"files\": $FILES, \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\", \"commit\": \"${{ github.sha }}\"}" > metrics/complexity.json

      - name: Check for New Complexity Issues
        if: github.event_name == 'pull_request'
        run: |
          ERRORS=${{ steps.complexity.outputs.errors }}

          # For PRs, we could compare against main branch
          # For now, just report
          if [ "$ERRORS" -gt 50 ]; then
            echo "⚠️ High complexity: $ERRORS errors detected"
            echo "Consider refactoring large files before merging"
          fi

      - name: Complexity Summary
        run: |
          ERRORS=${{ steps.complexity.outputs.errors }}
          WARNINGS=${{ steps.complexity.outputs.warnings }}
          FILES=${{ steps.complexity.outputs.files }}

          echo "### 📐 Code Complexity" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Files Checked | $FILES |" >> $GITHUB_STEP_SUMMARY
          echo "| Errors (large files/functions) | $ERRORS |" >> $GITHUB_STEP_SUMMARY
          echo "| Warnings (nesting/unwraps) | $WARNINGS |" >> $GITHUB_STEP_SUMMARY

      - name: Upload Complexity Metrics
        uses: actions/upload-artifact@v6
        with:
          name: complexity-metrics
          path: |
            metrics/
            complexity_output.txt
          retention-days: 90

  # Binary Size Tracking
  binary-size:
    name: Binary Size
    runs-on: macos-14
    needs: build-and-test

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      # Install dependencies (customize for your project)
      # - name: Install Dependencies
      #   run: brew install <your-dependencies>

      - name: Cache DerivedData
        uses: actions/cache@v5
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: deriveddata-${{ runner.os }}-${{ hashFiles('**/*.swift') }}
          restore-keys: |
            deriveddata-${{ runner.os }}-

      - name: Build Release
        run: |
          xcodebuild build \
            -project YourProject.xcodeproj \
            -scheme YourScheme \
            -configuration Release \
            -destination 'platform=macOS' \
            -derivedDataPath build \
            -quiet

      - name: Measure Binary Size
        id: size
        run: |
          APP_PATH=$(find build -name "YourApp.app" -type d | head -1)

          if [ -z "$APP_PATH" ]; then
            echo "❌ Could not find YourApp.app"
            exit 1
          fi

          # Get size in bytes
          SIZE_BYTES=$(du -sk "$APP_PATH" | cut -f1)
          SIZE_KB=$SIZE_BYTES
          SIZE_MB=$(echo "scale=2; $SIZE_KB / 1024" | bc)

          echo "size_kb=$SIZE_KB" >> $GITHUB_OUTPUT
          echo "size_mb=$SIZE_MB" >> $GITHUB_OUTPUT

          echo "📦 App Size: ${SIZE_MB} MB (${SIZE_KB} KB)"

      - name: Compare with Baseline
        id: compare
        run: |
          CURRENT_KB=${{ steps.size.outputs.size_kb }}
          BASELINE_FILE=".claude-team/metrics/binary-size-baseline.txt"

          if [ -f "$BASELINE_FILE" ]; then
            BASELINE_KB=$(cat "$BASELINE_FILE" | tr -d '[:space:]')
            DIFF=$((CURRENT_KB - BASELINE_KB))
            PCT_CHANGE=$(echo "scale=1; ($DIFF * 100) / $BASELINE_KB" | bc)

            echo "baseline_kb=$BASELINE_KB" >> $GITHUB_OUTPUT
            echo "diff_kb=$DIFF" >> $GITHUB_OUTPUT
            echo "pct_change=$PCT_CHANGE" >> $GITHUB_OUTPUT

            echo "📊 Baseline: ${BASELINE_KB} KB"
            echo "📊 Current:  ${CURRENT_KB} KB"
            echo "📊 Change:   ${DIFF} KB (${PCT_CHANGE}%)"

            # Warn if size increased by more than 10%
            if [ "$DIFF" -gt 0 ]; then
              THRESHOLD=10
              PCT_INT=${PCT_CHANGE%.*}
              if [ "${PCT_INT:-0}" -gt "$THRESHOLD" ]; then
                echo "⚠️ Binary size increased by more than ${THRESHOLD}%!"
                echo "bloat=true" >> $GITHUB_OUTPUT
              fi
            fi
          else
            echo "📝 No baseline found - this is the first measurement"
            echo "baseline_kb=0" >> $GITHUB_OUTPUT
          fi

      - name: Binary Size Summary
        run: |
          SIZE_MB=${{ steps.size.outputs.size_mb }}
          BASELINE_KB=${{ steps.compare.outputs.baseline_kb }}
          DIFF_KB=${{ steps.compare.outputs.diff_kb }}
          PCT=${{ steps.compare.outputs.pct_change }}

          echo "### 📦 Binary Size: ${SIZE_MB} MB" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$BASELINE_KB" != "0" ] && [ -n "$BASELINE_KB" ]; then
            if [ "${DIFF_KB:-0}" -gt 0 ]; then
              echo "⚠️ **+${DIFF_KB} KB (+${PCT}%)** from baseline" >> $GITHUB_STEP_SUMMARY
            elif [ "${DIFF_KB:-0}" -lt 0 ]; then
              echo "✅ **${DIFF_KB} KB (${PCT}%)** smaller than baseline" >> $GITHUB_STEP_SUMMARY
            else
              echo "✅ No change from baseline" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "📝 First measurement - no baseline to compare" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Save Size Artifact
        run: |
          mkdir -p metrics
          echo "${{ steps.size.outputs.size_kb }}" > metrics/binary-size.txt
          echo "{\"size_kb\": ${{ steps.size.outputs.size_kb }}, \"size_mb\": ${{ steps.size.outputs.size_mb }}, \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\", \"commit\": \"${{ github.sha }}\"}" > metrics/binary-size.json

      - name: Upload Size Metrics
        uses: actions/upload-artifact@v6
        with:
          name: binary-size
          path: metrics/
          retention-days: 90

  # Memory Safety Check (ASan + TSan)
  memory-safety:
    name: Memory Safety
    runs-on: macos-14
    needs: build-and-test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      # Install dependencies (customize for your project)
      # - name: Install Dependencies
      #   run: brew install <your-dependencies>

      - name: Cache DerivedData
        uses: actions/cache@v5
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: deriveddata-${{ runner.os }}-${{ hashFiles('**/*.swift') }}
          restore-keys: |
            deriveddata-${{ runner.os }}-

      - name: Run Address Sanitizer
        continue-on-error: true
        run: |
          echo "🔍 Running Address Sanitizer..."
          xcodebuild test \
            -project YourProject.xcodeproj \
            -scheme YourScheme \
            -destination 'platform=macOS' \
            -skip-testing:YourProjectUITests \
            -enableAddressSanitizer YES \
            -quiet 2>&1 | tee asan_output.txt || true

          if grep -q "AddressSanitizer" asan_output.txt; then
            echo "⚠️ Address Sanitizer found issues"
            grep -A 5 "AddressSanitizer" asan_output.txt | head -30
          else
            echo "✅ No memory issues detected"
          fi

      - name: Run Thread Sanitizer
        continue-on-error: true
        run: |
          echo "🔍 Running Thread Sanitizer..."
          xcodebuild test \
            -project YourProject.xcodeproj \
            -scheme YourScheme \
            -destination 'platform=macOS' \
            -skip-testing:YourProjectUITests \
            -enableThreadSanitizer YES \
            -quiet 2>&1 | tee tsan_output.txt || true

          if grep -q "ThreadSanitizer" tsan_output.txt; then
            echo "⚠️ Thread Sanitizer found race conditions"
            grep -A 5 "ThreadSanitizer" tsan_output.txt | head -30
          else
            echo "✅ No race conditions detected"
          fi

      - name: Memory Safety Summary
        run: |
          echo "### 🧠 Memory Safety Check" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if grep -q "AddressSanitizer\|ThreadSanitizer" asan_output.txt tsan_output.txt 2>/dev/null; then
            echo "⚠️ Issues detected - review logs" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No memory or threading issues detected" >> $GITHUB_STEP_SUMMARY
          fi
